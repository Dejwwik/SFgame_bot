# Required — Web UI login credentials
WEB_USERNAME=admin
WEB_PASSWORD=changeme

# Recommended — Flask session secret key
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-in-production
